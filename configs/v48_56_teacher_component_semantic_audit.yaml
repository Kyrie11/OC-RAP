# v48.56 TCSA: teacher/source correctness configuration.
# This file intentionally changes *teacher label construction*, not planner capacity.
teacher_margin_semantics:
  mode: strict_min_slack
artifact:
  # Primary WOMD teacher correctness should not use hand-coded hidden-branch
  # margins. Synthetic oracle-artifact fixtures may override this explicitly.
  use_margin_override: false
selection:
  gamma_rec: 0.0
  fixed_gamma_rec: 0.0
