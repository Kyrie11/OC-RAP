external_baselines:
  baseline: racp_lite
  methods: [racp_lite]
  max_candidates: 24
  policy:
    gamma_H: 0.0
    gamma_D: 5.0
    gamma_branch_rec: 0.0
    cvar_alpha: 0.20
    racp_risk_tolerance: 0.60
    racp_belief_temperature: 0.70
    racp_utility_weight: 1.0
    racp_branch_rec_weight: 1.0
    racp_belief_branch_weight: 0.45
    racp_tail_weight: 0.5
    racp_risk_weight: 2.5
    racp_smoothness_weight: 0.10
    racp_hard_weight: 10.0
    racp_harm_weight: 1.5
    racp_risk_threshold: 0.75
    risk_hard_weight: 1.0
    risk_harm_weight: 0.25
  training:
    device: auto
    batch_size: 64
    epochs: 1
