OC-RAP v48.57 DCP-DRFC-BCDE-CMRI validation status
Date: 2026-08-21

Algorithm / logic checks
- CMRI is zero-parameter and does not rewrite or retrain raw root logits.
- CMRI enters the actual native recovery certificate / native advantage evidence path, not diagnostics only.
- Eligibility is fail-closed: complete group, unique nominal, identical root_valid support; malformed/singleton/support-mismatch uses legacy root measure.
- Nominal anchor is proposal-set addition/removal/reordering invariant (no candidate-set mean).
- Checkpoint -> inference reconstruction records and verifies the CMRI flag.
- Factor cache / stage architecture / model contract include CMRI to prevent stale-cache reuse.
- v48.56-A reference reuse contract checks source hashes, protocol, dataset contract, scene-disjointness, no-test, and exact A semantics; uploaded v48.56-A reference passes.

Tests
- tests/test_v48_57_common_measure_root_invariance.py: 7 passed.
- v48.45-v48.57 mechanism regression: 119 passed, 3 failed only because the uploaded original code package itself lacks these historical operator text artifacts:
  * OC-RAP-v48.45.3-source-rebuild-and-SOWR-run-commands-ZH.txt
  * OC-RAP-v48.45.4-source-rebuild-and-SOWR-run-commands-ZH.txt
  * OC-RAP-v48.45.5-protocol-bootstrap-and-SOWR-run-commands-ZH.txt
  The same three files are absent from the uploaded v48.56 source tree; they were not fabricated.
- python -m compileall -q src tools: PASS.
- bash -n on all 134 scripts/*.sh: PASS.
- reference reuse preflight against uploaded v48.56-A: PASS / valid=true.

Runtime / experimental execution
- No GPU training was executed in this sandbox; the authoritative v48.57 result requires the user's WOMD-derived datasets and source checkpoints on their machine.
- End-to-end root-source decomposition therefore remains a runtime output of the supplied launcher, not a claimed pre-run result.
- v48.56 telemetry indicates scheduling imbalance rather than memory/compute capacity: the v48.57 launcher reuses valid A and runs B Balanced/Precision plus root-source audits across GPU0/GPU1 concurrently.
