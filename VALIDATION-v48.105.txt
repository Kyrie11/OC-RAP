V48.105 OC-PAEL validation

V48.90-95 regression:
  61 passed

V48.96-105 focused regression:
  65 passed

Total independently rerun selected tests:
  126 passed

V48.105 dedicated tests:
  6 passed (included above)

compileall src/tools/tests:
  PASS

shell scripts bash -n:
  182 checked, 0 failures

V48.105 runtime preflight:
  valid=true
  attribution_ready=true
  errors=[]
  audit_only=true
  stage_i/root_decoder/source/planner trainable parameters=0
  summary partition / candidate zero delta / agent permutation / action-interaction dimension synthetic checks=true

Comparator + pipeline smoke:
  PASS using V48.102 metrics as synthetic V48.105 inputs solely to exercise gate and provenance code paths.
  Synthetic status was PRELAST_ACTION_EQUIVARIANCE_LOCALIZATION_STOP; this is NOT a V48.105 scientific result.

Target-machine V48.105 experiment:
  NOT run in this sandbox because the historical L80 checkpoints and indexed feature datasets are not included in the uploaded result bundle.
