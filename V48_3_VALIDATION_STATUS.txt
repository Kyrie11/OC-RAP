OC-RAP v48.3 OC-TRAC-NASC/RCD validation status
Date: 2026-07-25

Passed:
- python -m compileall -q src tools
- bash -n scripts/train_ocrap_v48_trac_sr.sh
- model NASC forward/backward smoke test
- RCD loss forward/backward smoke test
- PYTHONPATH=src pytest -q: 119 passed, 4 warnings

New targeted regression coverage:
- nominal-anchored set context is permutation equivariant
- singleton/malformed set falls back to pointwise path
- regret-consistent distillation prefers the teacher-best recovery candidate

Not run in this environment:
- real WOMD v1.3.1 dataset construction
- Waymax/JAX GPU rollouts
- A30 training
- proxy/dedicated calibration
- Natural gate and closed-loop evaluation

Therefore algorithmic effectiveness is unverified until the supplied server commands complete.
