# V48.99 归因与 V48.100 OC-JRSD 设计

## 结论摘要

V48.99.0 OC-RJCA 工程可靠，可以正式归因。严格按预注册：state chart preserved=PASS；support-Jacobian=STOP；reserve/debt-Jacobian=STOP；full recovery-Jacobian=STOP；final source 不授权。

这不是“完全没有 action signal”。V48.99 相对 V48.98 进一步降低 dense reserve/debt delta Huber（约 3.4–4.9%），并在 dev-Contact 与 certificate-Near 显著提高 true-vs-shuffle action ordering；support/reserve 的 within-group top-1 也在多数 cell 有结构。但 pooled AUC 仍跨 split/regime 不稳定，尤其 dev-Near reserve 与 certificate-Contact reserve/support 没有形成统一坐标。

因此本轮最关键结论是：**冻结 recovery chart 后再拟合 state-conditioned post-decoder Jacobian 仍不足以得到 control-sufficient recovery representation。问题已从“Jacobian 是否 state-dependent”进一步收紧为“root representation 与 recovery chart 必须联合学习，使同一坐标系同时满足静态 state sufficiency 和 action equivariance”。**

## V48.99 可靠性

- uploaded runtime code SHA 与实际运行 contract 全部一致；
- pipeline 中 balanced/precision/PT/comparison/runtime artifact SHA 全闭合；
- balanced / precision PT 均只有 1,404 个 RJCA 参数：basis_raw [192,2]、action_map [2,125]、state_map [2,385]；
- planner/source/ERSS/Stage-I 参数均为 0；
- evaluation population 与 V48.98/V48.97.2/V48.96 exact match；
- nominal support/reserve identity error 均为 0；
- state AUC 与 V48.98 exact identity；
- 当前上传代码的 V48.97/98/99 tests 独立重跑 23/23 PASS。

因此 V48.99 的 STOP 可以归因到其机制本身。

## 严格预注册结果

### Support Jacobian

只有 balanced/certificate-Near 通过完整 AUC + permutation gate。其余 cell 虽有大量正的 true-minus-shuffle/top-1 结构，但 AUC 未达到统一阈值，因此正式 STOP。

### Reserve/debt Jacobian

通过的 cell 为 balanced + precision 的 dev-Contact 与 certificate-Near，共 4/8，只覆盖 2 个 unique roles，低于 6/8 + >=3 roles 的预注册要求，因此正式 STOP。

### Full

state preserved + support STOP + reserve STOP -> `RECOVERY_JACOBIAN_ALIGNMENT_STOP`。不授权 final source。

## 与 V48.98 的机制差异

V48.99 将全局 tangent 升级为 observation-conditioned Jacobian，并对语义坐标做固定 RMS metric normalization。结果表明：

- reserve/debt 连续回归进一步改善；
- support delta 的 dense error 继续恶化；
- 多数 role 的 within-group ranking 有信息；
- 但跨 scene/group 的坐标 calibration 不稳定。

这说明“local control direction”部分存在，但把静态 chart 固定后再拟合 dynamics 会受到 coordinate mismatch。一个控制充分状态不应只满足 `Z(o)` 能预测当前语义，还必须让 `Z(o,a)-Z(o,a0)` 在同一坐标中稳定表达动作效果。

## Main stack

继续保留：

1. OC-MERO observation-consistent deployability；
2. actuator-realizable executable recovery；
3. counterfactual equivalence-partition probability-mass transport；
4. identifiability before capacity；
5. support-establishment / signed reserve-debt semantics；
6. recovery base-state chart；
7. role-isolated RIFA。

V48.99 RJCA 不 promotion 为 Main，因为 full gate 失败。其长期价值是负结论：**post-decoder state-conditioned low-rank Jacobian 仍不足以形成 control-sufficient chart。**

## Dominant bottleneck

更新为：

> **dynamical/control sufficiency of the recovery representation**：同一个 observation-conditioned root measure / semantic chart 必须同时表达当前 support/reserve state 与 candidate-induced support/reserve change，并在 Near/Contact 与 dev/certificate 上稳定复制。

Near 与 Contact 的剩余问题与这个 bottleneck 是同一个目标，而不是两套 policy：

- Near：base state 已经可用，但 candidate-induced support 与 reserve direction 在不同 calibration variant 下不稳定；
- Contact：局部能分别学到 re-establish support 或 repay debt，但没有一个跨 group/split 稳定的 joint semantic coordinate。

## 下一步：V48.100 OC-JRSD

全称：**Observation-Consistent Joint Root-Semantic Decoder**。

V48.100 不再增加 tangent/Jacobian adapter，而是把表示 map 与 semantic chart 联合学习：

`(observation, action) -> frozen structured memory -> root measure -> [D,R]`。

唯一 trainable state：

- zero-init root-query displacement `delta_q [1,8,192]`：1,536 params；
- V48.97 ERSS chart：770 params，初始化自 V48.97.2；
- total：2,306 params。

Root cross-attention/self-attention/FFN/root-logit head、structured encoder、planner/source/RIFA 全部冻结。

### 数学目标

同一 chart 同时满足：

`Z(o,a) ~= Z_teacher(o,a)`

以及

`Z(o,a)-Z(o,a0) ~= Z_teacher(o,a)-Z_teacher(o,a0)`。

四个目标（absolute support/reserve + delta support/reserve）都使用 training-only 固定 RMS scale 做 dimensionless Huber，等权平均，无 loss-weight sweep。

### 为什么不是 source/head 或 BARR

- 输出只有 D/R 两个已识别语义，不是 admission logit；
- 监督来自 dense DRS/R_dep，不是 aggregate source target；
- 不学习 root x action 高秩场；
- 不使用 root/option/regime id；
- 不做 root-slot correspondence；
- action dependence来自原 structured memory，经 root query 对 frozen decoder body 的 observation-conditioned读取。

### 预注册 gate

Evaluation population 与 V48.99/V48.98/V48.97.2/V48.96 exact match。

State GO：AUC >=0.70 in >=6/8 cells，>=3/4 roles，Near+Contact。

Support/Reserve GO：true AUC >=0.65 且 true-minus-shuffled >=+0.05 in >=6/8；>=3/4 roles含 Near+Contact；top1 lift >=+0.10 in >=4/8 且 Near+Contact。

Full GO 才授权唯一一次 final fixed-capacity observation-aligned source。

如果 state+reserve GO / support STOP，则只允许 hybrid support-guard adjudication；如果 state+support GO / reserve STOP，则只允许 supported reserve-flow adjudication；否则关闭 root-query+chart family，再单独预注册 root cross-attention semantic objective。
