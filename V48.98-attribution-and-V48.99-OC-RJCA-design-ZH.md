# V48.98 OC-ERTA 归因与 V48.99 OC-RJCA 设计

## 1. 可靠性判决

V48.98.0 `v48.98.0-OC-ERTA` 结果可以科学归因。

独立复核：
- runtime contract `valid=true / attribution_ready=true / errors=[]`；
- 6 个 runtime 文件 SHA 与上传 drop-in 逐一一致；
- pipeline 中 balanced/precision/PT/comparison/runtime SHA 与上传 artifact 一致；
- balanced/precision PT 均只有 634 个 tangent 参数：`tangent_basis_raw/prefix_param_map/prefix_state_map/control_map`；
- V48.98 的 state/support/reserve 评估 population 与 V48.97.2/V48.96 逐 cell 完全一致；
- nominal support/reserve 最大绝对误差为 0；state AUC 与 V48.97.2 完全一致；
- V48.92–98 focused test 43/43 PASS。

因此没有工程 blocker。

## 2. 严格预注册判决

`EXECUTABLE_RECOVERY_TANGENT_ALIGNMENT_STOP`。

- state chart preserved: PASS；
- support tangent: STOP，仅 precision/certificate-Contact 同时满足 AUC>=0.65 与 AUC-vs-shuffle>=0.05；
- reserve/debt tangent: STOP，5/8 cell 过 AUC+shuffle gate，未达到 6/8；
- final source authorization: NO。

## 3. 关键现象

V48.98 不是“完全没学到”。相对 V48.97.2：

| variant/split | reserve delta Huber | 相对变化 | support delta MAE | 相对变化 |
|---|---:|---:|---:|---:|
| balanced/dev | 0.6238 -> 0.5753 | -7.8% | 0.2053 -> 0.2147 | +4.6% |
| balanced/cert | 0.6183 -> 0.5529 | -10.6% | 0.2078 -> 0.2163 | +4.1% |
| precision/dev | 0.6100 -> 0.5710 | -6.4% | 0.2063 -> 0.2135 | +3.5% |
| precision/cert | 0.6002 -> 0.5463 | -9.0% | 0.2085 -> 0.2155 | +3.4% |

说明固定全局二维 tangent 能改善连续 reserve/debt 回归，却不能稳定表达 support establishment。

## 4. 机制解释

V48.98 实际假设的是一个全局 action Jacobian：对所有 observation/state，共享 `U B delta_a`。但恢复作用高度依赖当前 scene/root 状态：相同制动、转向或速度 profile 在不同几何/接触状态中可以增加或减少 recovery support/debt。因此真正的局部对象应为

`Delta Z ≈ J(o, Z0) Delta a`。

这与 control-affine / bilinear lifted dynamics 的基本结构一致：控制向量场依赖 state，而不是用一个全局常数 action matrix。

同时，之前上传的 teacher-PCD index 的 DRS 呈明显的边界/近二值结构；如果 state-dependent Jacobian 最终能解决 reserve、但 support 继续失败，则下一分支应将 support 视为 guard/boundary crossing，而不是继续强迫它成为 smooth tangent。

## 5. 新关闭族

V48.98 后关闭：
- global centered rank-2 Stage-I tangent `U B delta_a`；
- V48.98 rank/width/token-block/LR/epoch sweep；
- 添加 macro/regime/category shortcut 救 tangent；
- 回 source/head、DRS/DEP threshold、support/reserve mixture；
- boundary transport、relative ranker、proposal expansion、dataset reconstruction。

“action tangent”作为数学问题不关闭；关闭的是 state-independent 的 V48.98 实现。

## 6. Dominant bottleneck

收紧为：

> **state-dependent, cross-regime action-equivariant recovery dynamics**：给定同一个 observation-conditioned recovery base state，candidate physical action delta 是否能诱导稳定的 shared-support / signed reserve-debt change。

## 7. Near / Contact

Near：reserve/debt 在 V48.98 已出现更稳定的正信号，但 support-establishment tangent 仍低于门槛；下一步优先解决 action 对 shared-support 的 observation-dependent 影响，而不是 Near 专属 threshold/router。

Contact：support 与 debt 在不同 split 中各自出现局部信号，但无法由同一个全局 tangent 统一。需要 state-conditioned Jacobian，而不是 Contact 专属 expert。

两者是同一个问题，不需要 regime-conditioned policy。

## 8. V48.99 OC-RJCA

全称：**Observation-Consistent Recovery-Jacobian Control-Affine Alignment**。

冻结：L80 encoder/transformer/root decoder、V48.97.2 ERSS base-state chart、source、RIFA/relative ranker。

只训练 decoded-root representation 上的 observation-conditioned control-affine Jacobian：

`eta = (A delta_a) odot C [1, LN(h_D0), LN(h_R0)]`

`r'_k = r_k + alpha_D(k) eta_D u_D + alpha_R(k) eta_R u_R`

其中：
- `h_D0/h_R0` 是 V48.97 冻结 nominal semantic states；
- `alpha_D/alpha_R` 是 V48.97 frozen semantic attention measures；
- `[u_D,u_R]` 是正交二维 basis；
- action 只使用 `prefix_param/prefix_state/control`；
- nominal `delta_a=0`，更新严格为 0；
- root permutation equivariant，无 root-slot matching。

参数量固定 1,404：

`2*192 + 2*125 + 2*(1+2*192) = 1404`。

### 语义 metric

D 与 R 的单位/尺度不同，因此 raw equal-Huber 不是 coordinate invariant。V48.99 用训练 teacher delta 的 RMS 只计算一次固定尺度：

`scale_D = RMS(Delta D*)`
`scale_R = RMS(Delta R*)`

训练 normalized Huber，不做 weight sweep。

## 9. V48.99 预注册

评估 population 与 V48.98/V48.97.2/V48.96 必须 exact match；nominal/state AUC 必须完全不变。

Support/Reserve 两个 gate 均维持：
- AUC>=0.65 且 true-shuffle >=0.05：>=6/8 cell；
- >=3/4 unique roles，Near+Contact；
- top1 lift >=0.10：>=4/8，Near+Contact。

GO：只授权一次 final fixed-capacity source。

Reserve GO / Support STOP：只授权 hybrid support-guard adjudication，不回 source。

Support GO / Reserve STOP：只审计 supported reserve flow。

两者 STOP：关闭 state-conditioned low-rank root Jacobian，进入 joint root-decoder semantic objective。

## 10. 主机制组合

当前长期保留：

`OC-MERO`
`+ actuator-realizable executable recovery`
`+ counterfactual equivalence-partition transport`
`+ identifiability before capacity`
`+ support-establishment / signed reserve-debt semantics`
`+ V48.97 recovery base-state chart`
`+ RIFA role isolation`。

V48.98 global action tangent 不 promotion。V48.99 recovery Jacobian 只有实验 GO 后才进入主机制组合。
