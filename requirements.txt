# Core runtime dependencies
numpy>=1.23
PyYAML>=6.0
torch>=2.0

# Development / validation
pytest>=8.0

# Optional acceleration for WOMD TFRecord CRC checks. The code has a pure Python fallback.
crc32c>=2.3

# Optional only when parsing real WOMD Scenario bytes:
# provide waymo_open_dataset.protos.scenario_pb2 via your local WOMD/proto installation.
