OC-RAP v48.48 NCP-DRFC validation status
Date: 2026-08-14

Scientific/engineering regression:
- v48.37-v48.48 selected regression: 115 passed
- v48.36 OCAF: 14 passed, 1 skipped
- v48.36.2 stage-transfer: 6 passed
- v48.36.3 terminal-state: 6 passed
- v48.36.4 idempotent terminal-state: 6 passed
- Unique completed total: 147 passed, 1 skipped

Static/packaging prechecks:
- python -m compileall -q src tools tests: PASS
- shell scripts bash -n: 109/109 PASS
- operator command bash -n: PASS
- same-local-builtin nounset dependency audit: 0 findings

v48.48-specific contracts covered:
- Native Certificate Preservation adds no learned state_dict parameters.
- NCP DRS uses the paper-facing hard observation-class q_best>=0 predicted-DRS boundary.
- NCP candidate-vs-nominal DRS/deployability margins match component-teacher sign convention.
- NCP overwrites only DRS/deployability component coordinates; gap/other evidence are unchanged.
- NCP method consumes no bucket/Safe/Near/Contact identifier.
- train config -> checkpoint metadata -> inference reconstruction contains NCP flag/tolerances.
- DRFC witness explicitly disables downstream NCP, preventing factor leakage into the witness causal factor.
- v48.48 factor matrix is exact A=(0,0), B=(1,0), C=(0,1), D=(1,1).
- 2x2 launcher runs two arms concurrently on distinct GPUs and defaults to serial Balanced/Precision within each arm.
- launcher checks free VRAM before starting an arm and records scheduling/provenance.
- comparator requires all four arms pipeline_valid with RC in {0,20}; engineering failures cannot enter attribution.

Uploaded v48.47 engineering diagnosis:
- A/B/D authoritative RC=20, pipeline_valid=true.
- C authoritative RC=30, pipeline_valid=false, certificate/gate not executed.
- C Balanced DRFC failure signature: torch.OutOfMemoryError at first train backward.
- Failing A30: 23.60 GiB total, 24.44 MiB free; one pre-existing process held ~20.86 GiB.
- Failing process itself had only ~124 MiB PyTorch allocated (+~50 MiB reserved), so this is resource contention, not DRFC numerical memory growth.
- D executed the same DRFC path successfully.

Limitations:
- This container does not expose the user's A30 GPUs or /data0 WOMD/Waymax datasets.
- Therefore no claim is made that a real v48.48 GPU/certificate run has already returned RC0/20.
