OC-RAP v48.35.1 RC30-TRAINING-CONTRACT-HOTFIX local validation
======================================================================

PASS: 17 focused v48.35/v48.35.1 tests
PASS: 174 supported release-matrix tests
WARN: 6 non-fatal PyTorch warnings in the release matrix
PASS: 57 shell scripts under scripts/ passed bash -n
PASS: python -m compileall -q src tools tests
PASS: tools/check_v48_35_frontier_contract.py
PASS: tools/check_v48_35_training_contract.py --help
PASS: tools/check_v48_35_resume_contract.py --help
PASS: repair wrapper --help exits without launching an experiment
PASS: patch -p1 --dry-run against the uploaded v48.35 code baseline

Focused tests cover:
- new exact-eligibility metadata;
- checkpoint-proven legacy repair;
- rejection of explicit false metadata;
- rejection when one checkpoint disables exact eligibility;
- rejection of controller/final checkpoint hash mismatch;
- exact known RC=30 signature authorization;
- resume authorization before status cleanup;
- adaptation skip/no-retraining behavior;
- train/dev index fail-closed behavior;
- operator wrapper argument handling.

Continuous-frontier preflight checks:
- smooth cap never exceeds free logit or safety cap;
- finite gradients;
- nominal physical-relative vector is zero;
- candidate representation is candidate minus nominal;
- absolute ego/scalar/shared scene suffix shortcuts excluded;
- regime ID not required;
- one shared policy contract.

Supported release matrix:
- core selector/runtime and CLI tests;
- v39 through v48.11 supported model/runtime contracts;
- v48.33, v48.34, v48.34.1, v48.35, v48.35.1;
- v50 runtime/recovery compatibility.

Known archive limitation:
The uploaded v48.35 result ZIP contains status, logs, summaries and SHA records but
omits checkpoint .pt bytes. Consequently the real no-retraining resume authorization
cannot be completed against the archive alone. This is intentional fail-closed
behavior: run check_v48_35_resume_contract.py on the original experiment machine.
If any of the six registered checkpoints is missing or has a different SHA/config,
resume is rejected and a fresh v48.35.1 run is required.

Historical-test boundary:
The uploaded source ZIP retains tests that reference v48.12-v48.32 historical shell
scripts that are not present in the upload. A raw all-history pytest therefore is not
a valid release criterion for this package. The delivery does not fabricate missing
historical assets.

Not executed locally:
- real WOMD tf_example loading;
- Waymax closed-loop simulation;
- two-A30 adaptation;
- shared adaptation-dev rule fitting on the user's data;
- Near/Contact certificate;
- Safe paired non-inferiority;
- held-out stress/test.

No claim is made that v48.35 passes the algorithm gate until the repaired pipeline
returns pipeline_valid=true with RC=0 or RC=20.
