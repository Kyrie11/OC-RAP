OC-RAP v48.20 UNISON-BRIDGE 本地验证状态
==========================================

验证日期：2026-07-30

PASS  pytest -q
      196 passed, 5 warnings

PASS  python -m compileall -q src tests tools

PASS  find scripts -type f -name '*.sh' -print0 | xargs -0 -n1 bash -n

PASS  v48.20 focused contracts
      - unified model does not use bucket/regime identity
      - nominal row pinning
      - exact conservative benefit min / harm max
      - component harm semantic reset
      - factorized masks and targets
      - group ERM cannot be replaced by candidate balance
      - deployment-exact frozen-top-k score
      - no safe-set gradient outside proposal top-k
      - legacy DUET/FACET compatibility

LIMITATION
      The delivery environment does not contain the user's real WOMD/Waymax
      datasets or two A30 GPUs. No v48.20 Natural-gate, stress, or closed-loop
      result was executed or claimed.
