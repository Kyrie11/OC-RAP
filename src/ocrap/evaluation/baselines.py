from __future__ import annotations

# Baseline hooks are intentionally thin wrappers around evaluator-time selection rules.
BASELINES = ["nominal", "risk_aware", "backup_filter", "contingency", "oracle_filter", "ocrap"]
