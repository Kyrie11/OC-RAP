OC-RAP v48.33 ELIGIBLE-SET-POLICY 本地验证状态
================================================

验证日期：2026-08-02
验证边界：当前环境不包含真实 WOMD/Waymax 数据、服务器训练 checkpoint 或两张 A30；以下仅证明代码、损失图、脚本与交付合同通过本地回归，不预先声明 gate 结果。

1. 多 proposal group 损失合同
--------------------------------
命令：
  PYTHONPATH="$PWD/src" python tools/check_v48_33_multigroup_loss_contract.py \
    --output /tmp/V48_33_MULTIGROUP_LOSS_CONTRACT.json

结果：PASS
- valid=true
- group_count=2
- loss=1.210774302482605，有限
- admission_gradient_l1=0.8067587614059448
- opportunity_gradient_l1=0.40104660391807556
- harm_gradient_l1=0.5346260666847229
- eligible_set_policy=true
- adaptive_margin=false
- strict_shape_contract=true
- outer_loop_collisions=[]

2. Python 单元与集成测试
--------------------------
命令：
  PYTHONPATH="$PWD/src" pytest -q

结果：PASS
- 301 passed
- 5 warnings
- 18.35 s

警告均为既有 PyTorch Transformer nested-tensor/测试张量转换警告，没有测试失败。

3. Python 编译
---------------
命令：
  python -m compileall -q src tools tests

结果：PASS

4. Shell 语法
--------------
命令：
  对 scripts/ 下全部 .sh 执行 bash -n

结果：PASS
- 检查脚本数：200
- 语法失败：0

5. v48.33 脚本依赖闭包
------------------------
结果：PASS
- v48.33 入口脚本：7
- Python/Shell 引用：48
- 缺失引用：0

详细结果：OC-RAP-v48.33-script-dependency-audit.json

6. 关键协议回归
----------------
结果：PASS
- strict fit 与 verify 阈值分离；
- GATE_SPEC、训练合同、校准合同和 runtime 的 proposal_top_k 均为5；
- selection semantics 固定为 rank_topk_then_filter_then_evidence_rerank；
- hard checkpoint metric 先过滤 eligible set 再 evidence rerank；
- soft checkpoint distribution 纳入 continuous eligibility；
- 没有 eligible candidate 时回退 nominal；
- eligible-set policy 对 benefit/opportunity、harm component、admission 三类头均产生非零梯度；
- adaptive teacher-gap margin 默认关闭；
- admission-only Stage-3 默认关闭；
- RC=30/20/0 后续实验授权保持 fail-closed。

7. 发布验证
------------
- v48.32.1 -> v48.33 补丁 clean-apply：PASS；
- 补丁应用目录与发布目录字节级/权限一致：PASS（627项）；
- ZIP 完整性：PASS；
- SHA256：PASS。
