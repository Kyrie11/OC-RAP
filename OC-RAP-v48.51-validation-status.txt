OC-RAP v48.51 DCP-DRFC-BCDE — validation status
Date: 2026-08-18

STATUS: CODE-LEVEL / CONTRACT-LEVEL VALIDATED FOR REAL-MACHINE EXPERIMENT
REAL A30 + /data0 END-TO-END RESULT: NOT EXECUTED IN THIS CONTAINER

Algorithm / attribution contract
- Strict 2x2: A=(BC-FC off, BC-NAP off), B=(on,off), C=(off,on), D=(on,on).
- v48.50 old DEFC disabled in all v48.51 arms.
- v48.50 exact-only NAP disabled in all v48.51 arms.
- smooth NAP retained in all arms.
- MC-NCP/native margin-complete preservation disabled in all arms.
- proposal_top_k remains 5.
- no new tuned threshold; BC-NAP reuses native_positive_gain=0.015 as materiality boundary.
- training/evaluation option semantics remain observation_class.
- strategy_regime_conditioning=false; no Safe/Near/Contact router/threshold/budget.
- test_roots_read=false during attribution.

Targeted regression results already run on this delivery
1) v48.51 BC-DE mechanism tests:
   4 passed.
   Covers material hard-sign preservation, smooth deadband order, parameter-free BC-NAP,
   exact-only/BC-NAP mutual exclusion, BC-FC boundary-depth sensitivity, and 2x2 isolation.

2) v48.47-v48.51 algorithm-focused regression group:
   40 passed, 11 warnings.

3) v48.34.1/v48.35.1/v48.35.2/v48.36.2 model/training/engineering/stage-transfer group:
   32 passed.

4) v48.36.3/v48.36.4/OCAF terminal-state/idempotent group:
   26 passed, 1 skipped.

5) v48.45 engineering + stage-isolation group:
   25 passed.

Targeted total: 123 passed, 1 skipped.
A single merged broad pytest invocation exceeded the container's 120 s execution window while
printing only passing progress; it is NOT counted as a pass or failure. The groups above were
therefore executed separately and are the reported authoritative code-level validation.

Fresh final sanity after all v48.51 code edits
- python -m compileall -q src tools tests: PASS
- all 118 *.sh files: bash -n PASS
- root OC-RAP-v48.51-DCP-DRFC-BCDE-two-GPU-run-commands-ZH.txt: bash -n PASS
- tests/test_v48_51_boundary_complete_de.py: 4 passed, 2 warnings

Fail-closed / engineering checks
- Synthetic D/Main authoritative RC=20: v48.51 post-gate wrapper blocks NEXT_COMMANDS execution: PASS.
- Synthetic authoritative RC=0 + valid D factor contract: post-gate wrapper authorizes gate-generated NEXT_COMMANDS: PASS.
- Synthetic v48.51 comparator with four isolated factor contracts: attribution_contract.valid=true: PASS.
- Recomputed v48.50 comparator on uploaded A/B/C/D result directories matched the uploaded 2x2
  audit semantically after excluding filesystem run paths: PASS.
- Two-GPU launcher uses distinct v48.51 telemetry/log prefixes; stale v48_50 launcher-log prefixes removed.
- Launcher attempts both A/B and C/D waves; any engineering RC outside {0,20} blocks attribution.
- Balanced/Precision remain serial within each arm by default; A/B and C/D use separate GPUs.

Known environment limitation
- This container does not provide the user's /data0/senzeyu2/dataset/OCRAP datasets or the target
  A30 runtime, so real v48.51 training/calibration/Natural-gate/closed-loop execution has not been
  claimed here. The provided command file is the next authoritative execution step on the user's machine.
