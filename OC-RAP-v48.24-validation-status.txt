OC-RAP v48.24 SUPPORT-BRIDGE 本地验证状态
================================================

python -m compileall -q src tools tests
  PASS

PYTHONPATH="$PWD/src" pytest -q
  220 passed, 5 warnings

find scripts -maxdepth 1 -type f -name '*.sh' -print0 | xargs -0 -n1 bash -n
  PASS

新增测试
  tests/test_v48_24_support_bridge.py：4 passed

静态合同检查
  PASS：shadow diagnostic 与 deployment strict path 分离
  PASS：runtime 读取 top-k/rerank/rank-margin/conditional-ranking
  PASS：v48.24 默认关闭 legacy Noisy-OR group opportunity
  PASS：消融最大并发为 2，每张 A30 一次一个任务

未执行
  真实 WOMD/Waymax v48.24 主训练与 certificate
  两张 A30 上的 adaptation-dev shadow/ablation
  held-out test/stress（且没有授权）

因此没有声称 v48.24 已通过 gate 或取得闭环改善。
